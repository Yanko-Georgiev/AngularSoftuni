export interface IUser {
    email: string,
    username: string,
    password: string,
    movies: string[],
    posts: string[], 
    _id: string[],
    created_at: string,
    updatedAt: string,
    _v: number,
}